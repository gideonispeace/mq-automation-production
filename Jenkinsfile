pipeline {
    agent any

    stages {
        stage('Verify Repo') {
            steps {
                sh '''
                    echo "Repo pulled successfully"
                    ls -la
                '''
            }
        }
    }
}
